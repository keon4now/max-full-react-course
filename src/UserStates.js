// //another way to filter conditional content
// let expensesContent = <p>No Expenses found.</p>

// if (filteredExpenses.length > 0) {
//   expensesContent = filteredExpenses.map((expense) => (
// 			<ExpenseItem
// 				key={expenses.id}
// 				title={expenses.title}
// 				amount={expenses.amount}
// 				date={expenses.date}
// 						/>
// 					))}

//           return (
//             <div>
//               {expensesContent}
//             </div>
//           )
