import ReactDOM from "react-dom/client";

import "./index.css";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />); // creates main interface that we will work on...main hook...tells react where UI should be placed

//index.html is located in public with root div...react is a single page so everything will be rendered there
